package service

import (
	"context"
	"lark/pkg/proto/pb_wallet"
)

func (s *walletService) Recharge(ctx context.Context, req *pb_wallet.RechargeReq) (resp *pb_wallet.RechargeResp, err error) {
	return
}
