package service

import (
	"context"
	"lark/pkg/proto/pb_wallet"
)

func (s *walletService) CheckBalance(ctx context.Context, req *pb_wallet.CheckBalanceReq) (resp *pb_wallet.CheckBalanceResp, err error) {
	return
}
